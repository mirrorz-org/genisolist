[zerotier-msi]
distro = ZeroTier
category = app
listvers = 1
location = zerotier/*.msi
pattern = zerotier/ZeroTier ?One\.msi
version = latest
platform = windows
type = msi

[zerotier-pkg]
distro = ZeroTier
category = app
listvers = 1
location = zerotier/*.pkg
pattern = zerotier/ZeroTier ?One\.pkg
version = latest
platform = macos
type = pkg
