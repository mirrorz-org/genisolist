[rust-analyzer]
distro = rust-analyzer
category = app
location = github-release/rust-analyzer/rust-analyzer/*/*
pattern = (\d{4}-\d{2}-\d{2})/rust-analyzer-([^/]+)\.([^/]+)$
version = $1
type = $3
platform = $2

